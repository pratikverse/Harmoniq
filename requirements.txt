# Runtime dependencies for the FastAPI inference API (src/api/main.py).
# Transitive deps (pydantic, starlette, scipy, python-dateutil, ...) are
# resolved by pip. The Streamlit app and the notebook/training stack are
# intentionally not listed here -- see requirements-dev.txt for those.

fastapi==0.116.1
uvicorn==0.35.0

pandas==2.3.1
numpy==2.1.3
pyarrow==21.0.0

scikit-learn==1.7.1
joblib==1.5.1
rapidfuzz==3.14.5
