# Runtime dependencies for the FastAPI inference API (src/api/main.py).
# Fully pinned (direct + transitive) so the Render build resolves identically
# every time. The Streamlit app and the notebook / training stack are not
# listed here -- see requirements-dev.txt.

# --- direct ---
fastapi==0.116.1
uvicorn==0.35.0
pandas==2.3.1
numpy==2.1.3
pyarrow==21.0.0
scikit-learn==1.7.1
joblib==1.5.1
rapidfuzz==3.14.5

# --- transitive (pinned for reproducibility) ---
annotated-types==0.8.0
anyio==4.15.1
click==8.5.0
h11==0.16.0
idna==3.19
pydantic==2.13.5
pydantic-core==2.46.5
python-dateutil==2.9.0.post0
pytz==2026.3.post1
scipy==1.18.1
six==1.17.0
starlette==0.47.3
threadpoolctl==3.6.0
typing-extensions==4.16.0
typing-inspection==0.4.4
tzdata==2026.3
