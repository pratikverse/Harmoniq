# Python generated files
__pycache__/
*.pyc
*.pyd
dist/
build/
eggs/
*.egg-info/
.ipynb_checkpoints/

# Virtual environment
music_venv/
venv/
env/
.venv/

# Data and model files (large and generated)
models/trained/

# models/catalog.parquet, embedding.npy, scaler.pkl, manifest.json are
# the deployed inference artifacts and are committed deliberately -- see
# docs/IMPROVEMENT_PLAN.md Phase 4.

# OS specific files
.DS_Store
Thumbs.db

# Log files
*.log

# Temporary files
*.tmp
*.swp
*.swo

# Jupyter Notebook temporary files
.jupyter/

# Kept locally only -- not pushed to GitHub, which only tracks what's
# actually deployed (frontend/, models/, src/api/, src/models/, plus
# render.yaml/requirements.txt/README.md). Everything below still
# exists on disk; it's just not part of the public repo history.
docs/
reports/
notebooks/
tests/
.streamlit/
.devcontainer/
.vscode/
.github/
src/ui/
src/training/
src/evaluation/
pyproject.toml
requirements-train.txt
.python-version
runtime.txt
